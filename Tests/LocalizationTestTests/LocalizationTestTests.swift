import XCTest
@testable import LocalizationTest

final class LocalizationTestTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LocalizationTest().text, "Hello, World!")
    }
}
